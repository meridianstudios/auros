# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env (Firebase config) — never commit real keys
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor (android/ is generated in CI via 'cap add android')
/android
resources/

# Play upload signing key — PRIVATE, never commit (debug.keystore is fine to commit, this is not)
ci/upload-keystore.jks
ci/UPLOAD-KEY-CREDENTIALS.txt
